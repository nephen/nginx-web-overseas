# Nginx SSL代理容器配置

# 容器名称
CONTAINER_NAME=nginx-cert

# 域名配置（多个域名用逗号分隔）
# 例如: yourdomain.com,www.yourdomain.com
DOMAINS=yourdomain.com,www.yourdomain.com

# 邮箱配置（用于SSL证书申请）
EMAIL=your-email@example.com

# 代理配置
# 反向代理的目标主机
PROXY_HOST=127.0.0.1

# 反向代理的目标端口
PROXY_PORT=2233

# 国家代码（用于SSL证书）
COUNTRY=US

# Nginx端口
NGINX_PORT=80