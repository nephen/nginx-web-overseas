# Nginx SSL代理容器配置

# 容器名称
CONTAINER_NAME=nginx-cert

# 域名配置（多个域名用逗号分隔）
# 例如: yourdomain.com,www.yourdomain.com
DOMAINS=yourdomain.com,www.yourdomain.com

# 邮箱配置（用于SSL证书申请）
EMAIL=your-email@example.com

# 代理配置
# 反向代理的目标主机
PROXY_HOST=127.0.0.1
# 反向代理的目标端口
PROXY_PORT=8080

# 国家代码（用于SSL证书）
COUNTRY=US

# Nginx端口
NGINX_PORT=80

# 可选配置
# 时区设置（可选，默认为UTC）
# TZ=Asia/Shanghai

# 是否启用staging模式（用于测试，可选，默认为false）
# STAGING=false

# 证书续期提前天数（可选，默认为30）
# RENEWAL_DAYS=30